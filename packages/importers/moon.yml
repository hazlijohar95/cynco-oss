$schema: 'https://moonrepo.dev/schemas/project.json'

# Pure data package: parses raw bank exports (CSV, OFX) into statement lines
# and draft ledger entries. No DOM, no React, no CSS, and no third-party
# runtime dependencies — the shared money-kernel shapes and the currency
# exponent table come from @cynco/ledger-core, the suite's one canonical
# definition of each.
language: 'typescript'
layer: 'library'
tags: ['tsdown', 'publishable', 'tier-domain']
